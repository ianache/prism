# Fixture directory

Each fixture is a raw binary file named by its lowercase fixture_id with a .bin suffix. The paired expected result is one canonical JSONL record. The directory is intentionally empty in the specification-only freeze; use docs/dataset-generation.md to create a temporary release artifact.

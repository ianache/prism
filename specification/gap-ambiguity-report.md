# Telemetry Specification Gap and Ambiguity Report

**Status:** Implementation gate closed. Every `BLOCKING` row must be resolved and reviewed before runtime, generator, broker, or benchmark-harness work begins.

## Blocking decision register

| ID | Topic | Current statement | Ambiguity | Impact | Proposed resolution | Status | Owner |
| --- | --- | --- | --- | --- | --- | --- | --- |
| FRAME-001 | Frame field widths and offsets | The canonical frame is `Header -> Core telemetry fields -> Bounded sensor/IO area -> Checksum`; it includes magic, version, flags, payload length, protocol, device identifier, timestamp, position, speed, heading, ignition, battery, bounded sensor/IO data, and checksum; all multi-byte values are little-endian. | Zero-based offsets, widths, variable-area encoding, length semantics, and exact ranges are undefined. | Bytes cannot be generated or decoded identically. | Freeze a versioned field table with offset, width, encoding, unit, inclusive range, required flag, and checksum coverage. | BLOCKING | Telemetry specification owner |
| FRAME-002 | Checksum algorithm and coverage | F1 validates checksum and the frame ends with a checksum. | Algorithm, initial value, polynomial/equivalent parameters, byte coverage, checksum byte order, and checksum-field inclusion are undefined. | Valid bytes and checksum-failure fixtures cannot be generated or verified. | Freeze algorithm, parameters, coverage range, field byte order, and rejection context. | BLOCKING | Telemetry specification owner |
| FRAME-003 | Timestamp epoch and unit | Core frame semantics include timestamp, and F3 performs timestamp normalization. | Epoch, integer unit, signedness, range, and output unit are undefined. | Expected normalized output cannot be compared cross-language. | Freeze epoch, integer unit, width, range, timezone behavior, and normalization equation. | BLOCKING | Telemetry specification owner |
| FRAME-004 | Coordinate fixed-point scale | Core semantics include position; F3 normalizes coordinates; canonical correctness uses integer/fixed-point values. | Latitude/longitude scale, signed encoding, bounds, rounding, and output unit are undefined. | Bytes, range validation, rules, and expected coordinates are not deterministic. | Freeze signed integer scale, inclusive bounds, conversion equation, and integer rounding rule. | BLOCKING | Telemetry specification owner |
| CONTRACT-001 | Contract schema version policy | Workflow, filter, data, and QoS contracts are canonical JSON documents validated with JSON Schema at control-plane/load time. | Schema identifier/version format, compatibility, reference resolution, and unsupported-version behavior are undefined. | Equivalent contracts may load differently and timed measurements may include inconsistent validation. | Freeze Draft 2020-12 identifiers, required `schema_version`, compatibility, load-time rejection, and reference resolution. | BLOCKING | PRISM contract owner |
| RULE-001 | Frozen deterministic rule table | F4 applies a frozen, versioned deterministic rule set with integer/fixed-point comparisons; F5 produces classification/severity; F6 produces a deterministic route enum. | Rule order, operands, units, operators, equality, missing sensors, conflicts, labels, severity, and route mapping are undefined. | Expected successful outputs cannot be generated or compared. | Publish a versioned ordered rule table with explicit precedence/equality/missing-value behavior and outputs. | BLOCKING | Telemetry domain owner |
| ERROR-001 | Stable error-code taxonomy | F1 rejects truncation, bad magic/version, length mismatch, range violation, unsupported protocol, and checksum failure; typed rejections have stable code/context. | Exact codes, precedence for multiple failures, required context, and separation from execution failures are undefined. | Expected rejection output cannot be compared reliably. | Freeze code catalog, validation order, required context schema, and execution-failure classification. | BLOCKING | PRISM contract owner |
| DATA-001 | Fixture and expected-result encoding | The dataset is deterministic, uses a recorded seed and approximately 100-, 250-, and 500-byte payloads; oracle fixtures compare logical values, not object layout. | Seed/PRNG, file encoding, manifest, digest, expected-result schema, ordering, and comparison normalization are undefined. | Identical bytes and expected outputs cannot be distributed or verified. | Freeze deterministic generation, binary/JSONL encoding, manifest/SHA-256 fields, expected-result schema, and comparison rules. | BLOCKING | Benchmark specification owner |
| BENCH-001 | Warm-up, sampling, and primary timer | Runs are warmed and steady-state; timer starts with a resident-ready frame and ends at final success/rejection; p99 is primary; B1/B2/B3 taxes use matched runs. | Warm-up threshold, repetitions, sampling, percentile algorithm, timer resolution, outlier policy, and tax percentage formula are undefined. | Latency and tax measurements are not comparable. | Freeze warm-up criteria, run/repetition count, sample format, percentile method, monotonic timer, outlier treatment, and tax formula. | BLOCKING | Benchmark methodology owner |
| BENCH-002 | Hardware/runtime metadata | Comparable runs record CPU, cores, RAM, OS/kernel, runtime/compiler, governor, container limits, commit, dataset digest, and command metadata. | Formats, collection commands, affinity/governor controls, equivalence criteria, and missing-metadata handling are undefined. | Comparable measurements cannot be audited or reproduced. | Freeze machine-readable metadata schema, collection procedure, required controls, and invalid-run policy. | BLOCKING | Benchmark methodology owner |
| QOS-001 | Provisional gate interpretation | P0 gates are p50 <= 250 microseconds, p95 <= 500 microseconds, p99 <= 1 millisecond, bounded memory/no sustained growth, no material sustained-load degradation, and B1 p99 workflow tax <= 20%; P1 is later transport evaluation. | “Bounded,” “material degradation,” matched population, memory window, pass/fail aggregation, and revision authority are undefined. | Performance qualification and gate decisions are ambiguous. | Freeze metric windows, degradation threshold, aggregation, matched-run criteria, revision process, and P1 deferral boundary. | BLOCKING | PRISM performance owner |

## Non-blocking compatibility register

| ID | Topic | Current statement | Ambiguity | Impact | Proposed resolution | Status | Owner |
| --- | --- | --- | --- | --- | --- | --- | --- |
| COMPAT-001 | P2-P4 compatibility policy | P2 permits interactive HTTP/gRPC, databases, caches, retries, and tracing; P3 adds durable execution; P4 adds long-running/agentic execution where deterministic replay is not assumed. | Cross-profile schema evolution and adapter policy are not defined. | Later P2-P4 compatibility only; it does not change P0 bytes, expected output, or comparable P0/P1 measurements. | Define profile-specific compatibility/adaptation after P0/P1 evidence. | NON-BLOCKING | Architecture owner |

## Artifact map and release condition

[Telemetry workflow](telemetry-workflow.md) maps frame, data, filter, workflow, QoS, error, and rule semantics. [Benchmark protocol](benchmark-protocol.md) maps dataset, scenarios, timers, metrics, and reporting. [Acceptance criteria](acceptance-criteria.md) maps each P0/P1 gate to its metric definition.

Runtime implementation remains prohibited until every `BLOCKING` row is resolved, represented in follow-on schemas/fixtures, and reviewed.
